#include "stdafx.h"
#include <stdlib.h>

//295. Find Median from Data Stream _hard
class MedianFinder {
public:
	/** initialize your data structure here. */
	MedianFinder() {

	}
	//要构造成两个堆，左边是是大顶堆(取最大值)，右边是小顶堆。
	//保证两个条件，一：左边的所有值比右边的要小。二：两者个数不能相差2.(只能是0或1)
	void addNum(int num) {

	}

	double findMedian() {

	}
};

int main(){	

	system("pause");
	return 0;
}